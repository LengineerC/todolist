## Qt TodoList

### 配置项
#### `config.json`

`borderRadius`: 窗口圆角大小

`theme`： 主题色
- `light`：亮色（默认）
- `dark`：暗色

`todoWrapMode`：换行方式
- `force`：超出长度强制换行（默认）
- `word`：按词换行（过长的词可能显示不全）

### 自定义构建
- `Qt6`
- `C++17`